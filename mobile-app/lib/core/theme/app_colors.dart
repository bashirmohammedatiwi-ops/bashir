import 'package:flutter/material.dart';

/// لوحة ألوان "الحياة" — مستوحاة من نظافة وحيوية Nice One مع هوية خاصة.
class AppColors {
  AppColors._();

  // الهوية الأساسية (وردي راقٍ)
  static const Color primary = Color(0xFFE1306C);
  static const Color primaryDark = Color(0xFFB81E54);
  static const Color primaryLight = Color(0xFFFCE4EC);
  static const Color onPrimary = Color(0xFFFFFFFF);

  // ثانوي / لمسة ذهبية خفيفة
  static const Color accent = Color(0xFFC8A24A);

  // خلفيات
  static const Color scaffold = Color(0xFFF6F6F9);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color card = Color(0xFFFFFFFF);

  // نصوص
  static const Color textPrimary = Color(0xFF1A1A2E);
  static const Color textSecondary = Color(0xFF6B6B7B);
  static const Color textMuted = Color(0xFF9A9AA8);

  // حالات
  static const Color sale = Color(0xFFE53935);
  static const Color success = Color(0xFF2E9E5B);
  static const Color warning = Color(0xFFF5A623);
  static const Color star = Color(0xFFFFB400);

  // حدود وفواصل
  static const Color border = Color(0xFFECECF1);
  static const Color divider = Color(0xFFF0F0F4);

  static const Color shimmerBase = Color(0xFFEDEDF2);
  static const Color shimmerHighlight = Color(0xFFF8F8FB);
}
