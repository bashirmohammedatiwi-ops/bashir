# Copy to .env.production before building the admin exe (npm run dist)
NEXT_PUBLIC_API_BASE=https://api.yourdomain.com/api/v1
# Optional — defaults to API host without /api/v1
# NEXT_PUBLIC_MEDIA_BASE=https://api.yourdomain.com/media
